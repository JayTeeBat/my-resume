# Normalize text to LF in the repo; Git still checks out CRLF on Windows.
# Without this, files churn between a Windows checkout and Linux CI.
* text=auto eol=lf

# Binary assets must never be line-ending munged.
*.woff2 binary
*.pdf binary
*.png binary
